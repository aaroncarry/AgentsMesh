# Traefik Static Configuration for Development
# Auto-generated by dev.sh - project: agentsmesh-feature-runner-login-optimization

api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  grpc:
    address: ":9443"

providers:
  docker:
    exposedByDefault: false
    network: agentsmesh-feature-runner-login-optimization_default
    # Only discover containers with this project's label
    constraints: "Label(`com.docker.compose.project`, `agentsmesh-feature-runner-login-optimization`)"
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: DEBUG
  format: common

accessLog:
  format: common
